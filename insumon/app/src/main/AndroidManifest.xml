<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.insumon">
    <!-- 寫入手機權限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- 相機權限 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScannerFinalResult"
            android:label="@string/title_activity_main3"
            android:parentActivityName=".Scanner" />
        <activity
            android:name=".ScannerManualInput"
            android:label="@string/title_activity_main2"
            android:parentActivityName=".Scanner" />
        <activity
            android:name=".Chart"
            android:label="Chart"
            android:parentActivityName=".Scanner" />
        <activity
            android:name=".History"
            android:label="List"
            android:parentActivityName=".Scanner" />
        <activity
            android:name=".Scanner"
            android:label="@string/app_name">

        </activity>
        <activity android:name=".TakePicActivity" />
    </application>

</manifest>